apiVersion: v1
kind: Service
metadata:
  name: {{ include "mining-manager-api.fullname" . }}
  labels:
    {{- include "mining-manager-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mining-manager-api.selectorLabels" . | nindent 4 }}
