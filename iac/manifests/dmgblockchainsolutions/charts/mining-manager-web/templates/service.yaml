apiVersion: v1
kind: Service
metadata:
  name: {{ include "mining-manager-web.fullname" . }}
  labels:
    {{- include "mining-manager-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mining-manager-web.selectorLabels" . | nindent 4 }}
