apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mining-manager-web.fullname" . }}-test-connection"
  labels:
    {{- include "mining-manager-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mining-manager-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
